Requirements taken from https://github.com/miguelgrinberg/flasky/blob/master/requirements

Then install with:
pip install -r requirements/requirements.txt  (or whatever path to requirements it is)

Note that many packages may now have newer builds, so may want to re-pip install them afresh